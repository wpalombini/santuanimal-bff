{
  "name": "santuanimal-bff",
  "version": "0.0.1",
  "description": "Backend for SantuAnimal",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "start": "npm run build && serverless offline",
    "deploy": "npm run build && serverless deploy --stage prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "serverless-offline": "^11.0.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
